name: Merge request event
on:
  pull_request:
    types:
      - opened
    branches:
      - main
      - 'dev*'

jobs:
  info:
    name: Some example job
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello"
